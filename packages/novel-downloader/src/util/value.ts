/**
 * Created by user on 2020/1/1.
 */

/**
 * 設定物件屬性值 (支援點符號路徑)
 * Set object property value (supports dot notation path)
 */
import dotSetValue from 'set-value';
/**
 * 取得物件屬性值 (支援點符號路徑)
 * Get object property value (supports dot notation path)
 */
import dotGetValue from 'get-value';

export {
	dotSetValue,
	dotGetValue,
}

export default {
	dotSetValue,
	dotGetValue,
}
